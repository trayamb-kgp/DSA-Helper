/**
 * HTML -> markdown. Two rules that are not negotiable (D026):
 *   - LaTeX passes through verbatim
 *   - figures become [Figure: <alt> - not included], never dropped silently
 *
 * Implemented in phase 1.
 */
export {};
