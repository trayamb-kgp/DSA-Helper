export function Options() {
  return (
    <main className="options">
      <h1>DSA Helper — Settings</h1>
      <p className="muted">Scaffold only — settings arrive in phase 7.</p>
    </main>
  );
}
